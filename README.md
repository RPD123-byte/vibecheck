# Uncover

On-device facial-expression inference and context-aware interruption for the Codex desktop app.

The production implementation lives under `src/`. Research prototypes and local model/data
artifacts are intentionally excluded from version control and must not be imported by core code.
